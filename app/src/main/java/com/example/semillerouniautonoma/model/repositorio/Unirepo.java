package com.example.semillerouniautonoma.model.repositorio;

public class Unirepo {
    /*private final String TAG = getClass().getSimpleName();

    public MutableLiveData<List<Users>> requestUsers(){
        final MutableLiveData<List<Users>> mutableLiveData = new MutableLiveData<>();

        ApiService apiService = Api.referenciar().create(ApiService.class);
        apiService.getUsers().enqueue(new Callback<ArrayList<Users>>() {
            @Override
            public void onResponse(Call<ArrayList<Users>> call, Response<ArrayList<Users>> response) {
                if (response.isSuccessful() && response.body()!= null){
                    mutableLiveData.setValue(response.body());
                }
            }

            @Override
            public void onFailure(Call<ArrayList<Users>> call, Throwable t) {

            }
        });
        return mutableLiveData;
    }*/
}
