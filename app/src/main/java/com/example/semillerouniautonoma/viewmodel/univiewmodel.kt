package com.example.semillerouniautonoma.viewmodel

import androidx.lifecycle.ViewModel

class univiewmodel: ViewModel() {

    private var observador: observador = observador()
    fun callobservador(){
       // observador.
    }
}